#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {

    ios::sync_with_stdio(false),cin.tie(nullptr);

    int a,b;
    cin>>a>>b;

    if(a >= b) {
        cout<<"YES\n";
    } else {
        cout<<"NO\n";
    }

    return 0;
}